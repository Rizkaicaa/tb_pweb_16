<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title> 
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f1f1f1;
        }

        .container {
            width: 100%;
            max-width: 400px;
            padding: 20px;
        }

        .login-box {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .login-box h2 {
            margin: 0 0 15px;
            padding: 0;
            color: #333;
            text-align: center;
            text-transform: uppercase;
        }

        .form-group {
            margin-bottom: 20px;
            position: relative;
        }

        .input-field {
            display: block;
            width: 100%;
            padding: 10px;
            border: none;
            border-bottom: 2px solid #ccc;
            font-size: 16px;
        }

        .input-field:focus {
            outline: none;
            border-bottom-color: #007bff;
        }

        .input-label {
            position: absolute;
            top: 10px;
            left: 0;
            color: #777;
            font-size: 16px;
            pointer-events: none;
            transition: 0.3s ease all;
        }

        .input-field:focus ~ .input-label,
        .input-field:valid ~ .input-label {
            top: -10px;
            font-size: 12px;
            color: #007bff;
        }

        .submit-button {
            background-color: #007bff;
            color: #fff;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            display: block;
            width: 100%;
            padding: 10px;
        }

        .submit-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <!-- Form untuk mengedit profil -->
    <form class="edit-profile-form">
        <div class="form-group">
            <input type="email" name="floating_email" id="floating_email" class="input-field" placeholder=" " required />
            <label for="floating_email" class="input-label">Email</label>
        </div>
        <div class="form-group">
            <input type="text" name="floating_first_name" id="floating_first_name" class="input-field" placeholder=" " required />
            <label for="floating_first_name" class="input-label">First Name</label>
        </div>
        <div class="form-group">
            <input type="text" name="floating_last_name" id="floating_last_name" class="input-field" placeholder=" " required />
            <label for="floating_last_name" class="input-label">Last Name</label>
        </div>
        <div class="form-group">
            <input type="tel" pattern="[0-9]{3}-[0-9]{3}-[0-9]{4}" name="floating_phone" id="floating_phone" class="input-field" placeholder=" " required />
            <label for="floating_phone" class="input-label">Nomor Telefon</label>
        </div>
        <button type="submit" class="submit-button">Submit</button>
    </form>
</body>
</html>
