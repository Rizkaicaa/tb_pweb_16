<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .form-profil {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
        }

        .form-profil h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            text-align: center;
        }

        .form-group {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .form-group div {
            flex: 1 1 48%; /* Makes each item take up 48% of the width */
            margin-bottom: 20px;
        }

        dt {
            font-weight: 600;
            color: #555;
        }

        dd {
            margin: 0;
            padding: 5px 0;
            color: #333;
        }

        .buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }

        .buttons form {
            margin: 0;
        }

        button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            font-size: 14px;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="form-profil">
        <h3>Profil</h3>
        <div class="form-group">
            <div>
                <dt>Full Name</dt>
                <dd><%= user.nama %></dd>
            </div>
            <div>
                <dt>Email Address</dt>
                <dd><%= user.email %></dd>
            </div>
            <div>
                <dt>Phone Number</dt>
                <dd><%= user.no_hp %></dd>
            </div>
        </div>
        <div class="buttons">
            <form action="/kadep/dashboard" method="GET">
                <button type="submit">Back</button>
            </form>
            <form action="/kadep/edit-profil" method="GET">
                <button type="submit">Edit Profile</button>
            </form>
            <form action="/auth/ubahPassword" method="GET">
                <button type="submit">Change Password</button>
            </form>
        </div>
    </div>
</body>
</html>
