<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f3debe;
            margin: 0;
            padding: 0;
        }

        .dashboard h1 {
            padding-left: 20px;
        }

        .dashboard {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .grid {
            display: grid;
            gap: 1rem;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        }

        .relative {
            position: relative;
        }

        .p-6 {
            padding: 1.5rem;
        }

        .rounded-2xl {
            border-radius: 1rem;
        }

        .bg-white {
            background-color: #fff;
        }

        .shadow {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .dark\:bg-gray-800 {
            background-color: #c9bfb3;
        }

        .space-y-2 > :not(:first-child) {
            margin-top: 0.5rem;
        }

        .text-sm {
            font-size: 0.875rem;
        }

        .font-medium {
            font-weight: 500;
        }

        .text-gray-500 {
            color: #d4cbbd;
        }

        .dark\:text-gray-400 {
            color: #000000;
        }

        .text-3xl {
            font-size: 1.875rem;
            line-height: 2.25rem;
        }

        .dark\:text-gray-100 {
            color: #000000;
        }

        .flex {
            display: flex;
            align-items: center;
        }

        .space-x-1 > :not(:first-child) {
            margin-left: 0.25rem;
        }

        .space-x-2 > :not(:first-child) {
            margin-left: 0.5rem;
        }

        .w-4 {
            width: 1rem;
        }

        .h-4 {
            height: 1rem;
        }

        button {
            background-color: #cc5506;
            color: #fff;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            cursor: pointer;
            margin-left: 1rem;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #2d3748;
        }

        /* Positioning logout and profile buttons */
        form[action="/auth/logout"],
        form[action="/kadep/profil"] {
            position: absolute;
            top: 20px;
            right: 20px;
        }

        form[action="/kadep/profil"] {
            right: 120px;
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <h1>Dashboard</h1>
        <form action="/auth/logout" method="POST">
            <button type="submit">Logout</button>
        </form>

        <form action="/kadep/profil" method="GET">
            <button type="submit">Profile</button>
        </form>

        <div class="grid gap-4 lg:gap-8 md:grid-cols-3 p-8 pt-20">
            <div class="relative p-6 rounded-2xl bg-white shadow dark:bg-gray-800">
                <div class="space-y-2">
                    <div class="text-3xl dark:text-gray-100">Data Laboratorium</div>
                    <div class="flex items-center space-x-1 rtl:space-x-reverse text-sm font-medium ">
                        <span>4</span>
                    </div>
                </div>
            </div>

            <div class="relative p-6 rounded-2xl bg-white shadow dark:bg-gray-800">
                <div class="space-y-2">
                    <div class="text-3xl dark:text-gray-100">Data Aset</div>
                    <div class="flex items-center space-x-1 rtl:space-x-reverse text-sm font-medium ">
                        <span>20</span>
                    </div>
                </div>
            </div>

            <div class="relative p-6 rounded-2xl bg-white shadow dark:bg-gray-800">
                <div class="space-y-2">
                    <div class="text-3xl dark:text-gray-100">Data Perbaikan</div>
                    <div class="flex items-center space-x-1 rtl:space-x-reverse text-sm font-medium ">
                        <span>5</span>
                    </div>
                </div>
            </div>

            <div class="relative p-6 rounded-2xl bg-white shadow dark:bg-gray-800">
                <div class="space-y-2">
                    <div class="text-3xl dark:text-gray-100">Data Pembeliann</div>
                    <div class="flex items-center space-x-1 rtl:space-x-reverse text-sm font-medium text-green-600">
                        <span>2</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
